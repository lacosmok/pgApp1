
<div class="container">
    {{#each model as |vehicle|}}
        <div class="row">
            <div class="col-md-4">
                {{vehicle.name}}
            </div>
            <div class="col-md-2">
                {{vehicle.places}}
            </div>
        </div>

    {{/each}}
</div>

{{outlet}}
