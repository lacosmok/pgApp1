
<div class="container">
    {{#each model as |vehicle|}}
        <div class="row">
            <div class="col-md-4">
                {{vehicle.name}}
            </div>
            <div class="col-md-2">
                {{vehicle.places}}
            </div>
            <div class="col-md-1">
                <button type="button" {{action "removeVehicle" vehicle}}>usuń</button>
                {{#link-to 'edit' vehicle}}edytuj{{/link-to}}
            </div>
        </div>

    {{/each}}
</div>

{{outlet}}
